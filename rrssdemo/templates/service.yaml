apiVersion: v1
kind: Service
metadata:
  name: {{ template "rrssdemo.fullname" . }}
  labels:
    app: {{ template "rrssdemo.name" . }}
    chart: {{ template "rrssdemo.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "rrssdemo.name" . }}
    release: {{ .Release.Name }}
